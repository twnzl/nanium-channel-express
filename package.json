{
  "name": "nanium-channel-express",
  "version": "0.1.1",
  "description": "a nanium request channel based on express using a single endpoint",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "bundle": "npx nanium rm dist && npm run build && npx cp package.json dist && cp README.md dist && npm publish ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "nanium",
    "channel",
    "express"
  ],
  "files": [
    "index.js",
    "index.d.ts"
  ],
  "author": "Tobias Wenzel",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "nanium": "^0.11.6"
  },
  "devDependencies": {
    "@alex_neo/jest-expect-message": "^1.0.5",
    "@types/jest": "^27.0.2",
    "@types/jest-expect-message": "^1.0.3",
    "jest": "^27.3.1",
    "ts-jest": "^27.0.7",
    "tslint": "^6.1.3",
    "typescript": "^4.4.3"
  }
}
